---
import SectionPage from '../../../components/SectionPage.astro';
import { sections } from '../../../lib/legacy';

const page = sections.matlab;
---
<SectionPage
  source={page.source}
  section="matlab"
  title={page.title}
  description={page.description}
  heading="Contribuciones y publicaciones"
/>
