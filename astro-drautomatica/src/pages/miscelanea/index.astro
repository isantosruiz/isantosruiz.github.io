---
import SectionPage from '../../components/SectionPage.astro';
import { sections } from '../../lib/legacy';

const page = sections.miscelanea;
---
<SectionPage
  source={page.source}
  section="miscelanea"
  title={page.title}
  description={page.description}
/>
